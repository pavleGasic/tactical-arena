shadow$provide.module$node_modules$pixi_DOT_js$lib$rendering$renderers$gl$shader$utils$generateArraySyncSTD40 = function(global, require, module, exports) {
  var createUboElementsSTD40 = require("module$node_modules$pixi_DOT_js$lib$rendering$renderers$gl$shader$utils$createUboElementsSTD40");
  "use strict";
  exports.generateArraySyncSTD40 = function(uboElement, offsetToAdd) {
    const rowSize = Math.max(createUboElementsSTD40.WGSL_TO_STD40_SIZE[uboElement.data.type] / 16, 1), elementSize = uboElement.data.value.length / uboElement.data.size, remainder = (4 - elementSize % 4) % 4, data = 0 <= uboElement.data.type.indexOf("i32") ? "dataInt32" : "data";
    return `
        v = uv.${uboElement.data.name};
        offset += ${offsetToAdd};

        arrayOffset = offset;

        t = 0;

        for(var i=0; i < ${uboElement.data.size * rowSize}; i++)
        {
            for(var j = 0; j < ${elementSize}; j++)
            {
                ${data}[arrayOffset++] = v[t++];
            }
            ${0 !== remainder ? `arrayOffset += ${remainder};` : ""}
        }
    `;
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$rendering$renderers$gl$shader$utils$generateArraySyncSTD40.js.map
