shadow$provide.module$node_modules$pixi_DOT_js$lib$rendering$renderers$gpu$shader$utils$generateArraySyncWGSL = function(global, require, module, exports) {
  var createUboElementsWGSL = require("module$node_modules$pixi_DOT_js$lib$rendering$renderers$gpu$shader$utils$createUboElementsWGSL");
  "use strict";
  exports.generateArraySyncWGSL = function(uboElement, offsetToAdd) {
    const {size, align} = createUboElementsWGSL.WGSL_ALIGN_SIZE_DATA[uboElement.data.type], remainder = (align - size) / 4, data = 0 <= uboElement.data.type.indexOf("i32") ? "dataInt32" : "data";
    return `
         v = uv.${uboElement.data.name};
         ${0 !== offsetToAdd ? `offset += ${offsetToAdd};` : ""}

         arrayOffset = offset;

         t = 0;

         for(var i=0; i < ${size / 4 * uboElement.data.size}; i++)
         {
             for(var j = 0; j < ${size / 4}; j++)
             {
                 ${data}[arrayOffset++] = v[t++];
             }
             ${0 !== remainder ? `arrayOffset += ${remainder};` : ""}
         }
     `;
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$rendering$renderers$gpu$shader$utils$generateArraySyncWGSL.js.map
