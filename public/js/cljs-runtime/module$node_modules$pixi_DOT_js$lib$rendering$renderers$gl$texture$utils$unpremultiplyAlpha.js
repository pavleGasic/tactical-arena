shadow$provide.module$node_modules$pixi_DOT_js$lib$rendering$renderers$gl$texture$utils$unpremultiplyAlpha = function(global, require, module, exports) {
  exports.unpremultiplyAlpha = function(pixels) {
    pixels instanceof Uint8ClampedArray && (pixels = new Uint8Array(pixels.buffer));
    const n = pixels.length;
    for (let i = 0; i < n; i += 4) {
      var alpha = pixels[i + 3];
      0 !== alpha && (alpha = 255.001 / alpha, pixels[i] = pixels[i] * alpha + 0.5, pixels[i + 1] = pixels[i + 1] * alpha + 0.5, pixels[i + 2] = pixels[i + 2] * alpha + 0.5);
    }
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$rendering$renderers$gl$texture$utils$unpremultiplyAlpha.js.map
