shadow$provide.module$node_modules$pixi_DOT_js$lib$rendering$high_shader$compiler$utils$formatShader = function(global, require, module, exports) {
  exports.formatShader = function(shader) {
    let indent = "";
    return shader.split(/([\n{}])/g).map(a => a.trim()).filter(a => a.length).map(a => {
      let indentedLine = indent + a;
      "{" === a ? indent += "    " : "}" === a && (indent = indent.substr(0, indent.length - 4), indentedLine = indent + a);
      return indentedLine;
    }).join("\n");
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$rendering$high_shader$compiler$utils$formatShader.js.map
