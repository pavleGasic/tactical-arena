shadow$provide.module$node_modules$pixi_DOT_js$lib$rendering$renderers$shared$geometry$utils$transformVertices = function(global, require, module, exports) {
  exports.transformVertices = function(vertices, m, offset, stride, size) {
    const a = m.a, b = m.b, c = m.c, d = m.d, tx = m.tx;
    m = m.ty;
    offset || (offset = 0);
    stride || (stride = 2);
    size || (size = vertices.length / stride - offset);
    offset *= stride;
    for (let i = 0; i < size; i++) {
      const x = vertices[offset], y = vertices[offset + 1];
      vertices[offset] = a * x + c * y + tx;
      vertices[offset + 1] = b * x + d * y + m;
      offset += stride;
    }
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$rendering$renderers$shared$geometry$utils$transformVertices.js.map
