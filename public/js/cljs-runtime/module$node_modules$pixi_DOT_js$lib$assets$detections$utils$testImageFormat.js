shadow$provide.module$node_modules$pixi_DOT_js$lib$assets$detections$utils$testImageFormat = function(global, require, module, exports) {
  exports.testImageFormat = async function(imageData) {
    if ("Image" in globalThis) {
      return new Promise(resolve => {
        const image = new Image();
        image.onload = () => {
          resolve(!0);
        };
        image.onerror = () => {
          resolve(!1);
        };
        image.src = imageData;
      });
    }
    if ("createImageBitmap" in globalThis && "fetch" in globalThis) {
      try {
        const blob = await (await fetch(imageData)).blob();
        await createImageBitmap(blob);
      } catch (_e) {
        return !1;
      }
      return !0;
    }
    return !1;
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$assets$detections$utils$testImageFormat.js.map
