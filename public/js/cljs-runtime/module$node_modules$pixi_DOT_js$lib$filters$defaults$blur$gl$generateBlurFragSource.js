shadow$provide.module$node_modules$pixi_DOT_js$lib$filters$defaults$blur$gl$generateBlurFragSource = function(global, require, module, exports) {
  var _const = require("module$node_modules$pixi_DOT_js$lib$filters$defaults$blur$const");
  "use strict";
  exports.generateBlurFragSource = function(kernelSize) {
    const kernel = _const.GAUSSIAN_VALUES[kernelSize], halfLength = kernel.length;
    let fragSource = "in vec2 vBlurTexCoords[%size%];\nuniform sampler2D uTexture;\nout vec4 finalColor;\nvoid main(void)\n{\n    finalColor \x3d vec4(0.0);\n    %blur%\n}", blurLoop = "", value;
    for (let i = 0; i < kernelSize; i++) {
      let blur = "finalColor +\x3d texture(uTexture, vBlurTexCoords[%index%]) * %value%;".replace("%index%", i.toString());
      value = i;
      i >= halfLength && (value = kernelSize - i - 1);
      blur = blur.replace("%value%", kernel[value].toString());
      blurLoop += blur;
      blurLoop += "\n";
    }
    fragSource = fragSource.replace("%blur%", blurLoop);
    return fragSource = fragSource.replace("%size%", kernelSize.toString());
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$filters$defaults$blur$gl$generateBlurFragSource.js.map
