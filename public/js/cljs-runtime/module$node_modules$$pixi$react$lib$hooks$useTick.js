shadow$provide.module$node_modules$$pixi$react$lib$hooks$useTick = function(global, require, module, exports) {
  var invariant = require("module$node_modules$$pixi$react$lib$helpers$invariant"), useApplication = require("module$node_modules$$pixi$react$lib$hooks$useApplication"), useIsomorphicLayoutEffect = require("module$node_modules$$pixi$react$lib$hooks$useIsomorphicLayoutEffect");
  "use strict";
  exports.useTick = function(options) {
    const {app, isInitialised} = useApplication.useApplication();
    let callback, context, isEnabled = !0, priority;
    "function" === typeof options ? callback = options : (callback = options.callback, context = options.context, isEnabled = options.isEnabled ?? !0, priority = options.priority);
    invariant.invariant("function" === typeof callback, "`useTick` needs a callback function.");
    useIsomorphicLayoutEffect.useIsomorphicLayoutEffect(() => {
      if (isInitialised) {
        const ticker = app?.ticker, wasEnabled = isEnabled, previousContext = context, previousCallback = callback;
        isEnabled && ticker && ticker.add(callback, context, priority);
        return () => {
          wasEnabled && ticker?.remove(previousCallback, previousContext);
        };
      }
    }, [app?.ticker, callback, context, isEnabled, isInitialised, priority]);
  };
};

//# sourceMappingURL=module$node_modules$$pixi$react$lib$hooks$useTick.js.map
