shadow$provide.module$node_modules$$pixi$graphics$lib$utils$buildRectangle = function(global, require, module, exports) {
  exports.buildRectangle = {build(graphicsData) {
    var rectData = graphicsData.shape;
    const x = rectData.x, y = rectData.y, width = rectData.width;
    rectData = rectData.height;
    graphicsData = graphicsData.points;
    graphicsData.length = 0;
    0 <= width && 0 <= rectData && graphicsData.push(x, y, x + width, y, x + width, y + rectData, x, y + rectData);
  }, triangulate(graphicsData, graphicsGeometry) {
    graphicsData = graphicsData.points;
    const verts = graphicsGeometry.points;
    if (0 !== graphicsData.length) {
      var vertPos = verts.length / 2;
      verts.push(graphicsData[0], graphicsData[1], graphicsData[2], graphicsData[3], graphicsData[6], graphicsData[7], graphicsData[4], graphicsData[5]);
      graphicsGeometry.indices.push(vertPos, vertPos + 1, vertPos + 2, vertPos + 1, vertPos + 2, vertPos + 3);
    }
  }};
};

//# sourceMappingURL=module$node_modules$$pixi$graphics$lib$utils$buildRectangle.js.map
