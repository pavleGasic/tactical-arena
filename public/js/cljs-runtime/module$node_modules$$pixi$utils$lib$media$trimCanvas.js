shadow$provide.module$node_modules$$pixi$utils$lib$media$trimCanvas = function(global, require, module, exports) {
  var getCanvasBoundingBox = require("module$node_modules$$pixi$utils$lib$media$getCanvasBoundingBox");
  exports.trimCanvas = function(canvas) {
    const boundingBox = getCanvasBoundingBox.getCanvasBoundingBox(canvas), {width, height} = boundingBox;
    let data = null;
    if (!boundingBox.isEmpty()) {
      canvas = canvas.getContext("2d");
      if (null === canvas) {
        throw new TypeError("Failed to get canvas 2D context");
      }
      data = canvas.getImageData(boundingBox.left, boundingBox.top, width, height);
    }
    return {width, height, data};
  };
};

//# sourceMappingURL=module$node_modules$$pixi$utils$lib$media$trimCanvas.js.map
