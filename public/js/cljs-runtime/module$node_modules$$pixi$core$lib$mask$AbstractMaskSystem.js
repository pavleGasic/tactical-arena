shadow$provide.module$node_modules$$pixi$core$lib$mask$AbstractMaskSystem = function(global, require, module, exports) {
  class AbstractMaskSystem {
    constructor(renderer) {
      this.renderer = renderer;
      this.maskStack = [];
      this.glConst = 0;
    }
    getStackLength() {
      return this.maskStack.length;
    }
    setMaskStack(maskStack) {
      const {gl} = this.renderer, curStackLen = this.getStackLength();
      this.maskStack = maskStack;
      maskStack = this.getStackLength();
      maskStack !== curStackLen && (0 === maskStack ? gl.disable(this.glConst) : (gl.enable(this.glConst), this._useCurrent()));
    }
    _useCurrent() {
    }
    destroy() {
      this.maskStack = this.renderer = null;
    }
  }
  exports.AbstractMaskSystem = AbstractMaskSystem;
};

//# sourceMappingURL=module$node_modules$$pixi$core$lib$mask$AbstractMaskSystem.js.map
