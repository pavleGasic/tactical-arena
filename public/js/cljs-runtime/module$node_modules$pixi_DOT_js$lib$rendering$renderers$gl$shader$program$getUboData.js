shadow$provide.module$node_modules$pixi_DOT_js$lib$rendering$renderers$gl$shader$program$getUboData = function(global, require, module, exports) {
  exports.getUboData = function(program, gl) {
    if (!gl.ACTIVE_UNIFORM_BLOCKS) {
      return {};
    }
    const uniformBlocks = {}, totalUniformsBlocks = gl.getProgramParameter(program, gl.ACTIVE_UNIFORM_BLOCKS);
    for (let i = 0; i < totalUniformsBlocks; i++) {
      const name = gl.getActiveUniformBlockName(program, i), uniformBlockIndex = gl.getUniformBlockIndex(program, name), size = gl.getActiveUniformBlockParameter(program, i, gl.UNIFORM_BLOCK_DATA_SIZE);
      uniformBlocks[name] = {name, index:uniformBlockIndex, size};
    }
    return uniformBlocks;
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$rendering$renderers$gl$shader$program$getUboData.js.map
