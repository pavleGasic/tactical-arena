shadow$provide.module$node_modules$$pixi$graphics$lib$const = function(global, require, module, exports) {
  var LINE_JOIN = (LINE_JOIN2 => (LINE_JOIN2.MITER = "miter", LINE_JOIN2.BEVEL = "bevel", LINE_JOIN2.ROUND = "round", LINE_JOIN2))(LINE_JOIN || {}), LINE_CAP = (LINE_CAP2 => (LINE_CAP2.BUTT = "butt", LINE_CAP2.ROUND = "round", LINE_CAP2.SQUARE = "square", LINE_CAP2))(LINE_CAP || {});
  global = {adaptive:!0, maxLength:10, minSegments:8, maxSegments:2048, epsilon:1e-4, _segmentsCount(length, defaultSegments = 20) {
    if (!this.adaptive || !length || isNaN(length)) {
      return defaultSegments;
    }
    length = Math.ceil(length / this.maxLength);
    return length < this.minSegments ? length = this.minSegments : length > this.maxSegments && (length = this.maxSegments), length;
  }};
  exports.GRAPHICS_CURVES = global;
  exports.LINE_CAP = LINE_CAP;
  exports.LINE_JOIN = LINE_JOIN;
  exports.curves = global;
};

//# sourceMappingURL=module$node_modules$$pixi$graphics$lib$const.js.map
