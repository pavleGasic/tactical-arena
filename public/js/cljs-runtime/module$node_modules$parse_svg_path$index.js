shadow$provide.module$node_modules$parse_svg_path$index = function(global, require, module, exports) {
  module.exports = function(path) {
    var data = [];
    path.replace(segment, function(_, command, args) {
      _ = command.toLowerCase();
      args = (args = args.match(number)) ? args.map(Number) : [];
      "m" == _ && 2 < args.length && (data.push([command].concat(args.splice(0, 2))), _ = "l", command = "m" == command ? "l" : "L");
      for (;;) {
        if (args.length == length[_]) {
          return args.unshift(command), data.push(args);
        }
        if (args.length < length[_]) {
          throw Error("malformed path data");
        }
        data.push([command].concat(args.splice(0, length[_])));
      }
    });
    return data;
  };
  var length = {a:7, c:6, h:1, l:2, m:2, q:4, s:4, t:2, v:1, z:0}, segment = /([astvzqmhlc])([^astvzqmhlc]*)/ig, number = /-?[0-9]*\.?[0-9]+(?:e[-+]?\d+)?/ig;
};

//# sourceMappingURL=module$node_modules$parse_svg_path$index.js.map
