shadow$provide.module$node_modules$pixi_DOT_js$lib$scene$text$canvas$utils$fontStringFromTextStyle = function(global, require, module, exports) {
  const genericFontFamilies = "serif sans-serif monospace cursive fantasy system-ui".split(" ");
  exports.fontStringFromTextStyle = function(style) {
    const fontSizeString = "number" === typeof style.fontSize ? `${style.fontSize}px` : style.fontSize;
    let fontFamilies = style.fontFamily;
    Array.isArray(style.fontFamily) || (fontFamilies = style.fontFamily.split(","));
    for (let i = fontFamilies.length - 1; 0 <= i; i--) {
      let fontFamily = fontFamilies[i].trim();
      /(["'])[^'"]+\1/.test(fontFamily) || genericFontFamilies.includes(fontFamily) || (fontFamily = `"${fontFamily}"`);
      fontFamilies[i] = fontFamily;
    }
    return `${style.fontStyle} ${style.fontVariant} ${style.fontWeight} ${fontSizeString} ${fontFamilies.join(",")}`;
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$scene$text$canvas$utils$fontStringFromTextStyle.js.map
