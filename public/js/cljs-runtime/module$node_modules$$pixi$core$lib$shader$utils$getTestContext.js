shadow$provide.module$node_modules$$pixi$core$lib$shader$utils$getTestContext = function(global, require, module, exports) {
  var constants = require("module$node_modules$$pixi$constants$lib$index"), settings = require("module$node_modules$$pixi$settings$lib$index");
  const unknownContext = {};
  let context = unknownContext;
  exports.getTestContext = function() {
    if (context === unknownContext || context?.isContextLost()) {
      const canvas = settings.settings.ADAPTER.createCanvas();
      let gl;
      settings.settings.PREFER_ENV >= constants.ENV.WEBGL2 && (gl = canvas.getContext("webgl2", {}));
      gl || (gl = canvas.getContext("webgl", {}) || canvas.getContext("experimental-webgl", {}), gl ? gl.getExtension("WEBGL_draw_buffers") : gl = null);
      context = gl;
    }
    return context;
  };
};

//# sourceMappingURL=module$node_modules$$pixi$core$lib$shader$utils$getTestContext.js.map
