shadow$provide.module$node_modules$pixi_DOT_js$lib$rendering$high_shader$compiler$utils$addBits = function(global, require, module, exports) {
  var warn = require("module$node_modules$pixi_DOT_js$lib$utils$logging$warn");
  "use strict";
  exports.addBits = function(srcParts, parts, name) {
    if (srcParts) {
      for (const i in srcParts) {
        var id = i.toLocaleLowerCase();
        if (id = parts[id]) {
          let sanitisedPart = srcParts[i];
          "header" === i && (sanitisedPart = sanitisedPart.replace(/@in\s+[^;]+;\s*/g, "").replace(/@out\s+[^;]+;\s*/g, ""));
          name && id.push(`//----${name}----//`);
          id.push(sanitisedPart);
        } else {
          warn.warn(`${i} placement hook does not exist in shader`);
        }
      }
    }
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$rendering$high_shader$compiler$utils$addBits.js.map
