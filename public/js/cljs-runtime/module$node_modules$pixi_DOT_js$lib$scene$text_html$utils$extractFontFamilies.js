shadow$provide.module$node_modules$pixi_DOT_js$lib$scene$text_html$utils$extractFontFamilies = function(global, require, module, exports) {
  exports.extractFontFamilies = function(text, style) {
    function addFontFamily(fontFamily2) {
      dedupe[fontFamily2] || (fontFamilies.push(fontFamily2), dedupe[fontFamily2] = !0);
    }
    const fontFamily = style.fontFamily, fontFamilies = [], dedupe = {};
    text = text.match(/font-family:([^;"\s]+)/g);
    if (Array.isArray(fontFamily)) {
      for (let i = 0; i < fontFamily.length; i++) {
        addFontFamily(fontFamily[i]);
      }
    } else {
      addFontFamily(fontFamily);
    }
    text && text.forEach(match => {
      match = match.split(":")[1].trim();
      addFontFamily(match);
    });
    for (const i in style.tagStyles) {
      addFontFamily(style.tagStyles[i].fontFamily);
    }
    return fontFamilies;
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$scene$text_html$utils$extractFontFamilies.js.map
