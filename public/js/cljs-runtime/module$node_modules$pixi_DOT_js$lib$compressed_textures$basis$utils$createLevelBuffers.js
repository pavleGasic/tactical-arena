shadow$provide.module$node_modules$pixi_DOT_js$lib$compressed_textures$basis$utils$createLevelBuffers = function(global, require, module, exports) {
  exports.createLevelBuffers = function(basisTexture, basisTranscoderFormat) {
    const images = basisTexture.getNumImages(), levels = basisTexture.getNumLevels(0);
    if (!basisTexture.startTranscoding()) {
      throw Error("startTranscoding failed");
    }
    const levelBuffers = [];
    for (let levelIndex = 0; levelIndex < levels; ++levelIndex) {
      for (let sliceIndex = 0; sliceIndex < images; ++sliceIndex) {
        var transcodeSize = basisTexture.getImageTranscodedSizeInBytes(sliceIndex, levelIndex, basisTranscoderFormat);
        transcodeSize = new Uint8Array(transcodeSize);
        if (!basisTexture.transcodeImage(transcodeSize, sliceIndex, levelIndex, basisTranscoderFormat, 1, 0)) {
          throw Error("transcodeImage failed");
        }
        levelBuffers.push(transcodeSize);
      }
    }
    return levelBuffers;
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$compressed_textures$basis$utils$createLevelBuffers.js.map
