shadow$provide.module$node_modules$$pixi$core$lib$textures$resources$autoDetectResource = function(global, require, module, exports) {
  const INSTALLED = [];
  exports.INSTALLED = INSTALLED;
  exports.autoDetectResource = function(source, options) {
    if (!source) {
      return null;
    }
    let extension = "";
    if ("string" == typeof source) {
      var result = /\.(\w{3,4})(?:$|\?|#)/i.exec(source);
      result && (extension = result[1].toLowerCase());
    }
    for (result = INSTALLED.length - 1; 0 <= result; --result) {
      const ResourcePlugin = INSTALLED[result];
      if (ResourcePlugin.test && ResourcePlugin.test(source, extension)) {
        return new ResourcePlugin(source, options);
      }
    }
    throw Error("Unrecognized source type to auto-detect Resource");
  };
};

//# sourceMappingURL=module$node_modules$$pixi$core$lib$textures$resources$autoDetectResource.js.map
