shadow$provide.module$node_modules$pixi_DOT_js$lib$rendering$renderers$gpu$shader$utils$generateLayoutHash = function(global, require, module, exports) {
  exports.generateLayoutHash = function({groups}) {
    const layout = [];
    for (let i = 0; i < groups.length; i++) {
      const group = groups[i];
      layout[group.group] || (layout[group.group] = {});
      layout[group.group][group.name] = group.binding;
    }
    return layout;
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$rendering$renderers$gpu$shader$utils$generateLayoutHash.js.map
