shadow$provide.module$node_modules$pixi_DOT_js$lib$scene$graphics$shared$utils$triangulateWithHoles = function(global, require, module, exports) {
  var earcut = require("module$node_modules$earcut$src$earcut");
  "use strict";
  exports.triangulateWithHoles = function(points, holes, vertices, verticesStride, verticesOffset, indices, indicesOffset) {
    if (holes = earcut(points, holes, 2)) {
      for (let i = 0; i < holes.length; i += 3) {
        indices[indicesOffset++] = holes[i] + verticesOffset, indices[indicesOffset++] = holes[i + 1] + verticesOffset, indices[indicesOffset++] = holes[i + 2] + verticesOffset;
      }
      verticesOffset *= verticesStride;
      for (indices = 0; indices < points.length; indices += 2) {
        vertices[verticesOffset] = points[indices], vertices[verticesOffset + 1] = points[indices + 1], verticesOffset += verticesStride;
      }
    }
  };
};

//# sourceMappingURL=module$node_modules$pixi_DOT_js$lib$scene$graphics$shared$utils$triangulateWithHoles.js.map
